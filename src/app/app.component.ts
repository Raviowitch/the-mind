import { Component } from '@angular/core';
@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.scss'],
})
export class AppComponent {
  title = 'the-mind';

  constructor() {
    // board =
    // const board = new Board();
    // const p1 = new Player('p1', 'Hélèna', board.giveHand());
    // const p2 = new Player('p2', 'Mathieu', board.giveHand());
    // console.log(p1);
    // console.log(p2);
    // console.log(board);
    // p1.drawHand();
    // p2.drawHand();
  }
}
